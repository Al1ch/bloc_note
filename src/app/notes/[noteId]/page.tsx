import React from "react";
import SideBar from "@/components/SideBar/SideBar";

const NotesPage = ({ params }: { params: { userName: string } }) => {
  return <div>15</div>;
};

export default NotesPage;
